import chalk from 'chalk';

export async function fixCommand(_targetPath?: string, _options: Record<string, unknown> = {}): Promise<void> {
  console.log(chalk.yellow('imgclean fix will be available in Phase 4.'));
}
